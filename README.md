# Study Mate 🧠

**Study Mate** is an AI-powered adaptive learning platform designed to turn long notes into intelligent study materials. It leverages large language models (LLMs) to provide summaries, practice questions, revision cards, and concept dependencies.

## ✨ Key Features
- **Smart Summary**: Automatically condense lengthy notes into high-impact concepts.
- **Adaptive Practice**: Generate AI-powered Multiple Choice Questions (MCQs) for active recall.
- **Deep-Dive Revision**: Professional learning blocks structured for maximum exam recall.
- **Concept Graph**: Visualize dependencies between concepts to master your learning path.
- **Topic Statistics**: Track your mastery percentage across different subjects.
- **PDF Export**: Download your study sessions as clean, printable PDFs.

## 🚀 Tech Stack
- **Frontend**: React, Vite, Vanilla CSS (Midnight Neon branding)
- **Backend**: FastAPI (Python), SQLAlchemy (SQLite)
- **AI Engine**: Groq / OpenAI integration via LangChain/OpenAI SDK
- **Data Analysis**: Pandas for performance analytics

## 🛠️ Setup & Installation
1. Clone the repository:
   ```bash
   git clone https://github.com/ajithasri05/study-mate.git
   ```
2. Set up the Python environment:
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\\Scripts\\activate
   pip install -r requirements.txt
   ```
3. Set your environment variables:
   Create a `.env` file with:
   ```
   GROQ_API_KEY=your_key_here
   OPEN_AI_API_KEY=your_key_here
   ```
4. Start the backend:
   ```bash
   python -m server.main
   ```
5. Run the frontend:
   ```bash
   npm install
   npm run dev
   ```

---
*Generated by Study Mate AI*
